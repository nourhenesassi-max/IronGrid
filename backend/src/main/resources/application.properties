spring.application.name=IronGrid

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/IronGrid?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=nourhene
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server
server.port=8080

# JWT (keep as is)
app.jwt.secret=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_64_CHARS_MIN________________________
app.jwt.expirationMinutes=240
